<!DOCTYPE html>
<html lang="en" data-bs-theme="<%= settings.dark_mode ? 'dark' : 'light' %>">
<head>
  <meta charset="UTF-8">
  <title>Import / Export - <%= name %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; padding: 2rem; }
    .card { border-radius: 1rem; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
  </style>
</head>
<body class="p-4">
    <div class="container mt-4 mb-3">
          <ul class="nav nav-tabs">
            <li class="nav-item">
              <a class="nav-link" href="/">🏠 Home </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/tables">🧩 Tables</a>
            </li>
            <li class="nav-item">
              <a class="nav-link"  href="/tables/<%= name %>/structure/">📊 Structure</a>
            </li>
            <li class="nav-item">
              <a class="nav-link"  href="/table/<%= name %>/">🗄️ Data</a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="/table/<%= name %>/import-export/">📁 Import / Export</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">🧰 Tools</a>
            </li>
            <li class="nav-ietm">
              <a href="/settings" class="nav-link">⚙️ Settings</a>
            </li>
          </ul>
  </div>
  <div class="container">
    <a href="/table/<%= name %>/structure" class="btn btn-secondary mb-4">← Back to Structure</a>
    <h2>📁 Import / Export: <%= name %></h2>

    <div class="row mt-4 g-4">
      <!-- Export Section -->
      <div class="col-md-6">
        <div class="card p-4">
          <h4>⬇️ Export Data</h4>
          <p>You can export this table as a CSV or JSON file.</p>
          <a href="/table/<%= name %>/export/csv" class="btn btn-outline-primary mb-3">Export as CSV</a>
          <a href="/table/<%= name %>/export/json" class="btn btn-outline-success">Export as JSON</a>
        </div>
      </div>

      <!-- Import Section -->
      <div class="col-md-6">
        <div class="card p-4">
          <h4>⬆️ Import Data</h4>
          <p>Upload a CSV file to import data into this table.</p>
          <form action="/table/<%= name %>/import/csv" method="POST" enctype="multipart/form-data">
            <input type="file" name="file" accept=".csv" class="form-control mb-3" required>
            <button type="submit" class="btn btn-outline-primary">Import CSV</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
